# Example environment variables for the frontend (do not commit .env with secrets)

# SITE_URL is used by auth email redirects (if integrating with external providers in the future)
REACT_APP_SITE_URL=http://localhost:3000
